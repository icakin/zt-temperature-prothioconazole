# Claude Code prompt — add a Tm knob; recast the decomposition to match the elasticity (equal, range-fair, nominal-centred); IQR-band TPC plots; reconcile the report (autonomous)

Run from the project root (`.../MICROADAPT/etcGEMs`). Run AFTER #19 (equal-perturbation
elasticity) has committed. Growth only. Complete model (unfolding envelope + medium-matched
measured sector allocation).

NOTE TO USER: launch in an auto-approving mode so it runs unattended.

WHY: #19 showed the elasticity (equal standardised step, magnitude, at the operating point)
and the existing Shapley decomposition DISAGREE — the decomposition says "envelope dominates
every descriptor, the rate is now envelope-dominated (rmax phi_E=0.72)", while the elasticity
says rmax is ALLOCATION-controlled (envelope elasticity exactly 0) and Topt/Ea are coupled.
The disagreement is methodological: the decomposition (a) used UNEQUAL ranges (envelope wide:
dCp x0.5-2.0, dTopt +/-6 K; allocation narrow), which inflates the envelope share; (b) is NOT
centred on the measured nominal (samples f_metab 0.2-0.4 while nominal ~0.48); and (c) reports
variance SHARES, which conflate share with magnitude (CTmax is "99% envelope" of a negligible
movement). ALSO: neither analysis perturbs Tm, even though the report claims the envelope axis
perturbs "Topt/Tm" — so CTmax / the high-T falling limb (Tm-controlled) is untested. Fix all
of this and reconcile the two report sections into one story.

---

```
Work AUTONOMOUSLY end to end; commit in parts; print a summary. Read first:
src/etcgem/{enzyme_cost.py (Perturbation, the unfolding params path where per-enzyme Tm is
read ~L200-210),unfolding.py,sensitivity.py (run_elasticity from #19),decomposition.py,
tpc.py,plotting.py,cli.py}, the #19 outputs under
strains/eciML1515/outputs/elasticity_elasticity/ and the decomposition outputs under
strains/eciML1515/outputs/decompose_decomposition_sectors/, and reports/etcgem/report.qmd
(both the "What generates TPC variation (global sensitivity)" elasticity section AND the
"Allocation vs envelope decomposition — is the division of labour forced or emergent?"
section) + reports/etcgem/assemble.py.

PART A - add a Tm perturbation knob (mirrors dTopt)
- Add `dTm` to Perturbation: a uniform additive shift (K) applied to every enzyme's Tm,
  nominal 0. Wire it into the unfolding parameter path so a shifted Tm flows through
  unfolding_params -> native fraction f_N(T) -> cost. (Optionally add `tm_scale` for the
  Tm spread, mirroring topt_scale, but dTm is the priority.) Keep the additive-vs-multiplicative
  reference-scale handling consistent with #19: the standardised step for dTm uses a REFERENCE
  SCALE = the SD of the model's per-enzyme Tm distribution; record it.
- Verify: a positive dTm should push CTmax / the falling limb to higher T; a negative dTm
  should pull it in. Sanity-check before proceeding.

PART B - add Tm to BOTH analyses
- ELASTICITY (#19): add dTm as an input, perturbed by the same standardised step h. Report its
  elasticity across all descriptors (expect: large on CTmax and the hot-side descriptors,
  small on rmax/cold side). Re-run; update elasticity_table/heatmap/tornado/reference_scales.
- DECOMPOSITION: the envelope axis becomes {dTopt, topt_scale, dCp_scale, dTm}. Also report the
  envelope broken into a KINETIC sub-part (dTopt, topt_scale, dCp_scale) and a STABILITY
  sub-part (dTm), so we can see kinetic -> cold side/Ea vs Tm -> hot side/CTmax.

PART C - recast the decomposition to be range-fair and nominal-centred (match the elasticity)
- Perturb EVERY parameter (both axes) by the SAME standardised relative half-width H (config,
  e.g. H=0.2), CENTRED ON THE MEASURED NOMINAL: multiplicative params over
  nominal*(1 +/- H); additive params (dTopt, dTm) over +/- H*reference_scale; sector fractions
  over nominal*(1 +/- H) with the biosynthesis share renormalised. FIX the current mismatch
  where f_metab was sampled 0.2-0.4 but the nominal is ~0.48 — centre on the true nominal.
- Keep the crossed two-group functional-ANOVA / Shapley design (so the allocation x envelope
  INTERACTION term S_AE is retained — this is what the local elasticity can't give).
- Report, per descriptor, BOTH: (i) the variance shares (S_A, S_E, S_AE, phi_A, phi_E) AND
  (ii) a MAGNITUDE measure (e.g. the absolute achievable range / IQR of the descriptor under
  each axis). The magnitude is essential so a large share on a negligible movement (CTmax)
  cannot masquerade as importance.

PART D - the IQR-band TPC plot (the centerpiece)
- Make the key figure: per axis, the FAMILY of absolute TPCs (growth rate in 1/h, NOT peak-
  normalised) generated by perturbing that axis alone over the standardised range, summarised
  as the MEDIAN curve with a shaded INTERQUARTILE band. Show one panel/band per axis:
  (1) allocation, (2) kinetic envelope (Topt/curvature), (3) Tm / stability, and optionally
  (4) full envelope. Overlay the nominal TPC. This should make it visually obvious that
  allocation moves the HEIGHT, the kinetic envelope moves the cold side / Ea, and Tm moves the
  hot side / CTmax. Put this figure front-and-centre in the decomposition section.

PART E - run on the complete model
- Re-run the elasticity (with dTm) and the recast decomposition on eciML1515. Report the new
  numbers. EXPECTED (state whether borne out): rmax allocation-dominated (matching the
  elasticity's ~0 envelope effect); Topt/Ea coupled; CTmax now clearly Tm/stability-controlled
  with a real magnitude (no longer "insensitive to everything"); cold-side shape kinetic-
  envelope-led. Note explicitly how the recast numbers differ from the old decomposition and
  why (equal ranges + nominal-centring + magnitude + Tm added).

PART F - reconcile the report into ONE consistent narrative
- Rewrite BOTH sections so they agree. The elasticity and the recast decomposition now use the
  same equal, nominal-centred perturbations and the same parameter set (incl. Tm), so present
  them as two views of one result: elasticity = per-parameter magnitude/direction at the
  operating point; decomposition = grouped variance shares + interaction + achievable IQR bands.
- CORRECT the old decomposition claims: replace "the thermal envelope dominates every
  descriptor" and "the rate is now envelope-dominated too (phi_E=0.72)" with the range-fair
  result. Be explicit that the earlier conclusion was inflated by unequal, envelope-favouring
  ranges + off-nominal sampling + reporting shares without magnitude, and that under a range-
  fair, nominal-centred, magnitude-aware analysis the rate and optimum are allocation-led, the
  cold-side shape is kinetic-envelope-led, and the upper limit is Tm/stability-controlled.
- Keep the H1.3 framing but state the honest, nuanced answer: the split is only partly clean
  (rmax->allocation, CTmax->Tm are clean; Topt/Ea are coupled), and it is measure-dependent,
  which is itself the finding and reinforces why the measurements (allocation + meltome/Tm)
  matter. Lead the section with the IQR-band TPC figure.
- Update assemble.py; re-run assemble.py; `quarto render`; confirm the PDF builds with no
  unresolved crossrefs.

VERIFY (report all)
1. dTm works (CTmax moves with Tm); the Tm reference scale used.
2. Elasticity incl. dTm: dTm's elasticity per descriptor (confirm it dominates CTmax).
3. Recast decomposition (equal H, nominal-centred): per-descriptor phi_A/phi_E/S_AE AND the
   magnitude/IQR; how rmax, Topt, Ea, CTmax now come out, and how that differs from the old
   numbers (rmax phi_E was 0.72 -> now?).
4. The IQR-band TPC figure exists and shows the axis -> curve-region mapping.
5. Both report sections now tell one consistent story; PDF builds.

CONSTRAINTS
- Do NOT change the core MMRT/unfolding/enzyme-cost math beyond adding the dTm hook; add
  analysis + plumbing only.
- Equal, standardised, nominal-centred perturbations only (structural version). Do NOT
  implement the uncertainty-weighted ranges here — that stays the deferred follow-up.
- Keep the old (unequal-range) decomposition outputs only if useful, clearly labelled as
  superseded; otherwise move to supplementary.
- The write-up must match the actual implementation and numbers (read the outputs; use the
  real H, reference scales, and results).
- Autonomous; commit in parts: "add dTm unfolding perturbation knob",
  "Tm in elasticity + decomposition", "recast decomposition: equal nominal-centred ranges +
  magnitude + interaction", "IQR-band TPC figure", "report: reconcile sensitivity +
  decomposition into one narrative".
```
